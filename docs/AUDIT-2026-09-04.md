# TERRA 敵対的監査（2026-09-04）と修正指示プロンプト

監査者: Claude（前任セッション）。対象: https://factory-maker-floor.chalky-wasp-3685.chatgpt.site/?lang=ja + `claude/adlib-pivot` HEAD (d182846)。
手法: コードレビュー + Playwright実機検証（スクショ・DOM検査・タイマー競合の再現実験）+ IPCC AR6数値の検算。

## 監査結果サマリ

| # | 深刻度 | 指摘 | 証拠 | 対応状況 |
|---|---|---|---|---|
| 1 | **P0** | `earth_focus_region('japan')` で**北米が中央に来る**。テクスチャの経度写像とlatLon球面座標の不一致（経度反転/オフセット）で全リージョン照準ズレ | スクショ: 日本クリック後に北米西海岸+ハワイが中央。terra-audit-2-japan.png | **fixed** — 経度の符号をThree.jsのUV座標へ合わせ、日本フォーカスの距離も調整（`82d75cd`, `d99bf04`）。Playwrightで日本・北極・北ヨーロッパを再確認。 |
| 2 | **P0** | GMSL(海面)の`best`がAR6中央値でなく**レンジ算術平均**: 0.47=(0.32+0.62)/2, 0.60=(0.44+0.76)/2, 0.82=(0.63+1.01)/2。AR6 Table 9.9の中央値は0.44/0.56/0.77m。「評価済み科学情報」の看板に反する値の創作 | science.ts SCENARIOS.seaLevel2100 検算一致 | **fixed** — Table 9.9の中央値0.44/0.56/0.77mへ修正し、low/highは維持（`bcbbe12`）。 |
| 3 | **P0** | 海氷レイヤーのバッジが**2100年でも「観測値」**。実表示は2025年以降「説明用表示」(illustrative path)なのに静的ラベルが観測を主張 | 実機: 年2100で「北極海氷 観測値」表示を確認 | **fixed** — 2025年以前は「観測値」、2026年以降は「説明用表示」へ動的に切り替え（`bf71ba5`）。Playwrightで2100年表示を確認。 |
| 4 | P1 | 物語タイマーが**ユーザー/エージェントの操作を上書き**。実証: ユーザーがr6で海面を選択→4.8秒後に物語タイマーがAMOCへ強制変更(r7)。ツール説明の「interruptible」に反し、revision楽観ロック体系の外で書き込む | 実機再現ログ: `earth_set_layer(sea_level)→r6` の後 `story: North Atlantic circulation→r7` | **fixed** — 通常操作で保留タイマーを全停止し、物語ステップを`mutate()`経由へ統一（`5b76ef4`）。0.6秒後の割り込みから5.2秒待っても再開しないことをPlaywrightで確認。 |
| 5 | P1 | `earth_play_story('sea_level')` が**arcticシーケンスを再生**（startStoryが引数無視）。ツール契約の詐称 | EarthExperience.tsx startStory | **fixed** — 世界平均海面2050→日本周辺での世界平均海面2100という専用シーケンスを実装。地域浸水は主張しない（`fde039b`）。 |
| 6 | P1 | WorldLayer(WebGPU流体)が**不透明背景に完全遮蔽されたまま毎フレーム実行**（実測 frames=2021/34s）。視覚ゼロ・電池消費のみ。READMEの「fluid field runs behind the globe」は実態と不一致 | elementFromPoint=terra-globe / earth.css背景不透明 | **fixed** — 選択肢(b)を採用し、TERRAルートからWorldLayerをアンマウント。READMEの記述も実態へ合わせた（`7783c78`）。 |
| 7 | P1 | AMOC弱化34–45%は**シナリオ非依存で全SSPに同一適用**。Weijer et al. 2020のCMIP6範囲はシナリオ依存。SSP1-2.6でも−39.5%表示は過大 | science.ts amocForYear | **fixed** — 原著で確認した2100年のCMIP6モデル平均（SSP1-2.6: 24%、SSP2-4.5: 29%、SSP5-8.5: 39%）へ変更（`f9a0939`）。なお、34〜45%を高排出のみとした当初の監査記述は原著と一致しないため採用していない。 |
| 8 | P2 | AMOCレンジ表記「−11.3–15.0%」が符号曖昧（−11.3〜−15.0の意図） | 実機metrics | **fixed** — 34〜45%は不確実性レンジではないため表示自体を撤去し、シナリオ別平均値と簡略表示ラベルへ置換（`f9a0939`）。 |
| 9 | P2 | 年スライダーのドラッグで change毎にrevision++/ログ/TTS発火（連打スパム）。debounce無し | EarthExperience mutate/onChange | **fixed** — 表示値は即時更新し、最後の入力から250ms後に1回だけ確定する方式へ変更（`e47d197`）。 |
| 10 | P2 | GMSL簡略表示（線形進行）のタグが画面上に無い（AMOCは「reduced display」表記あり）。sea_levelメトリクスにも簡略表示明記が必要 | metrics UI | **fixed** — GMSLカードに「簡略表示 / Reduced-order display」を追加（`73dd0b2`）。 |
| 11 | P2 | 海流チューブ`closed:true`の戻り弧が大陸横断の白い航路状に見える。北極海氷シェーダの縞が市松アーティファクト | スクショ同上 | **fixed** — 曲線とTubeGeometryを`closed:false`へ変更し、海氷ノイズ係数を0.16から0.04へ低減（`49190c3`）。 |
| 12 | P2 | 地球の自転/粒子アニメが prefers-reduced-motion を無視（CSSのみ対応済み、three側未対応） | EarthGlobe animate | **fixed** — 動きを減らす設定ではslerp、カメラ補間、シェーダ時間変化、海流粒子移流を停止（`b48dcb5`）。 |
| 13 | P2 | `terra-error` 文言が英語固定（i18n漏れ） | EarthExperience | **fixed** — 日英のCOPYへ移管（`32de97b`）。 |
| 14 | P3 | dead code（GlobeRuntime.pointer未使用）、波形バーaria-hidden無し、canvas aria-labelが「Interactive」と主張するがドラッグ操作なし | EarthGlobe/UI | **fixed** — 直接操作の実装時にdead codeと波形バーを撤去し、canvasラベルを指定文言へ変更（`5022091`, `1d6c11a`）。 |
| 15 | 検証 | 気温値はAR6 Table SPM.1とほぼ一致（SSP1-2.6 nearのhigh=1.8のみ要突合。1.7の可能性） | science.ts | **verified** — Table SPM.1原典では1.2〜1.8℃。現行値1.8℃が正しいことを確認（`bcbbe12`）。 |

**良かった点**（維持すること）: 6ツールの契約設計とrevision楽観ロック、ナレーション文の限界明示、出典リンクグリッド、JA SSR、コンソールエラーゼロ、WebMCP非接続時の正直な表示、フォールバックUI。

## 修正後の受け入れ検証

Playwright 1.62.1とローカルのGoogle Chromeを使い、`http://localhost:3000/?lang=ja`で自動検証した。

| 検証項目 | 結果 | 証跡 |
|---|---|---|
| 日本・北極・北ヨーロッパの照準 | **passed** — 3地点を順に選択し、各地形が中央へ移動することを目視確認 | `docs/evidence/2026-09-04/playwright-japan-focus.png`, `playwright-arctic-focus.png`, `playwright-europe-focus.png` |
| 2100年の海氷ラベル | **passed** — メトリクスのバッジが「説明用表示」 | `docs/evidence/2026-09-04/playwright-sea-ice-2100-illustrative.png` |
| 物語の割り込み | **passed** — 再生0.6秒後に海面へ切り替え、5.2秒待機後も見出しが「世界の海面が上がる過程を見る。」のまま | `docs/evidence/2026-09-04/playwright-story-interrupted.png` |

最終静的検証: `npm run lint`, `npx tsc --noEmit`, `npm run build`。いずれも成功（buildのチャンク容量警告は失敗ではない）。

---

## Codexへの修正指示プロンプト（そのまま貼り付け）

```
TERRA（webmcp-factory, ブランチ claude/adlib-pivot）に対する敵対的監査で、科学的誠実性と主要機能に関わる欠陥が見つかった。docs/AUDIT-2026-09-04.md の表を正とし、以下を priority 順に修正せよ。締切日のため、P0→P1を完了してからP2に進み、各修正は小さくコミットして同一セッション内で push すること。

## P0（提出ブロッカー）

1. 【照準ズレ】earth_focus_region('japan') で北米が中央に来る。EarthGlobe.tsx の latLon() とテクスチャ経度写像（makeEarthTexture の x=(lon+180)/360）の座標系不一致が原因。three.js SphereGeometry のUV原点を踏まえ、latLon の theta 符号/オフセット、または globe の初期回転で整合させよ。
   受け入れ基準: Playwright で「日本」「北極」「欧州」を順にクリックし、スクショで各地形が画面中央に来ることを目視確認できる証跡を残す（日本列島が判別できること）。

2. 【海面best値の捏造】science.ts の seaLevel2100.best がレンジ算術平均になっている。IPCC AR6 WGI Table 9.9 の likely range 中央値に修正: SSP1-2.6 → 0.44、SSP2-4.5 → 0.56、SSP5-8.5 → 0.77（low/highは現状維持で正しい）。あわせて temperature の全数値を AR6 Table SPM.1 と突合し、根拠をコミットメッセージに「出典表: 値↔表番号」で明記せよ（SSP1-2.6 near の high=1.8 は 1.7 の疑いあり。必ず原典で確認）。

3. 【海氷「観測値」ラベル】レイヤーバッジを year 依存の動的表示にせよ: year <= 2025 は「観測値 / Observed」、それ以降は「説明用表示 / Illustrative」。terra-metrics やナレーションと矛盾しないこと。
   受け入れ基準: year=2100 で海氷レイヤーのバッジが「説明用表示」になっているスクショ。

## P1

4. 【物語のstomp】物語タイマー実行中に人間またはエージェントが mutate したら、pending の storyTimers を全てキャンセルせよ（mutate と executeCommand の冒頭で storyTimers.current.forEach(clearTimeout) + story:null）。物語ステップ自体も mutate() 経由に統一し、revision 体系の外で setScene しないこと。
   受け入れ基準: 物語再生→0.6秒後にレイヤー変更→5秒待って物語が続行しないことを Playwright で再現確認。

5. 【sea_level物語が未実装】startStory が story 引数を無視して常に arctic シーケンスを再生する。sea_level 用のステップ列（例: global/sea_level/2050 → japan/sea_level/2100 ※地域浸水を主張しない文言）を実装するか、時間がなければツール enum から 'sea_level' を削除して契約と実装を一致させよ。

6. 【遮蔽された流体】WorldLayer が不透明背景の裏で毎フレーム実行されている。二択: (a) terra-stage の背景を透過し、globe の背後に流体が見えるよう合成する（星空との共存を確認）、(b) TERRA では WorldLayer をアンマウントし、README の該当記述を削除。どちらでも「動いていないものを動いていると書かない」を満たすこと。

7. 【AMOCのシナリオ非依存】34–45%減は高排出シナリオ由来の範囲。最低限の修正として、AMOC メトリクスとナレーションに「SSP5-8.5相当の公表レンジに基づく簡略表示」と明記し、SSP1-2.6/SSP2-4.5 選択時は値を出すのをやめるか「このシナリオの評価値は表示していません」と表示する。Weijer et al. 2020 のシナリオ別数値を確認できるなら、シナリオ別レンジに置き換えるのが望ましい。

## P2（P0/P1完了後）

8. AMOCレンジ表記を「−15.0〜−11.3%」の形に修正（符号を両端に）。
9. 年スライダーの onChange を 250ms debounce し、確定値のみ revision/ログ/TTS を発火。
10. GMSL メトリクスカードに「簡略表示」タグを追加（AMOC と同様の small ラベル）。
11. 海流チューブは closed:false にして戻り弧を消す。海氷シェーダの市松状ノイズを弱める（sin積の係数を下げる）。
12. prefers-reduced-motion 時は自転 slerp とカレント粒子アニメを停止（静止フレームは描画してよい）。
13. terra-error を COPY 経由で i18n 化。
14. GlobeRuntime.pointer の dead code 削除、波形バーに aria-hidden、canvas の aria-label を「Three-dimensional Earth visualization」に修正。

## してはいけないこと
- 数値を「見栄えのため」に丸める・創作する（全て原典と突合し、出典をコードコメントに残す）
- revision 楽観ロックと6ツールの契約シグネチャを変える（エージェント互換を壊す）
- .env やシークレットに触れる、リポジトリを勝手に public 化する

## 完了条件
- npm run lint && npx tsc --noEmit がクリーン
- Playwright で: 日本照準スクショ / 海氷2100ラベル / stomp再現なし の3証跡を取得
- docs/AUDIT-2026-09-04.md の表に対応状況列（fixed/skipped+理由）を追記してコミット
```
